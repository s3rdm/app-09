import React from 'react';
import styled from 'styled-components';
import BaseWrapper from '../templates/baseWrapper';

const Login = () => (
  <BaseWrapper>
    <Inner>
      <p>Login</p>
    </Inner>
  </BaseWrapper>
);

const Inner = styled.div``;

export default Login;
